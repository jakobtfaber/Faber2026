# Inventory the live manuscript projection

- Type: `wayfinder:task` (AFK)
- Status: open
- Assignee: —
- Blocked by: —
- Map: [Manuscript-only Overleaf bridge](../map-overleaf-manuscript-bridge.md)
- Triage: `ready-for-agent`

## Question

Produce a read-only, timestamped inventory of the exact existing Overleaf
project and current GitHub `main`: immutable identities where available, every
path and byte size, the `main.tex` compile dependency closure, explicit files
needed for collaborator work, Overleaf-only paths, GitHub-only candidate
projection paths, per-file and aggregate editable size, and the current PDF
identity. Classify uncertainty; do not pull into authority, delete, upload,
compile with changed inputs, or expose credentials.
