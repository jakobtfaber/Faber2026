\section{Results}
\label{sec:results}

The association tests, the foreground census, and the dispersion-budget
decomposition have passed re-validation and are reported below. Other results
remain withheld: scattering fits for individual sightlines, scintillation
products, turbulence parameters, burst energies, and the measured-versus-predicted
scattering comparison. Those products still depend on the checks in the
circulation-readiness plan.

\FloatBarrier
\subsection{Association of the co-detections}
\label{sec:results-association}

All twelve candidate pairs pass the association tests of
Section~\ref{sec:toa}. Under the conservative, chance-maximizing windows of
Section~\ref{sec:toa-pcc}, every pair has a chance-coincidence probability
$P_{\rm cc}<10^{-8}$---fewer than one in ten million such pairings would be
expected to arise by chance---and passes the positional-coincidence check; the
timing residuals, referenced to
400\,MHz under the shared DSA-DM convention, are consistent with the
predicted inter-site geometric delay (Table~\ref{tab:sample}). The eight
bursts with constrained CHIME-side dispersion measures also pass the
CHIME--DSA DM-agreement check; the remaining four are retained as
position-and-timing associations, since their CHIME sub-band fits do not
provide a constraining independent DM. We therefore treat all twelve pairs as
genuine co-detections in what follows.

\FloatBarrier
\subsection{Per-sightline dispersion budget}
\label{sec:results-budget}
\label{sec:budget}

Table~\ref{tab:budget} reports the decomposition of Eq.~\ref{eq:dmbudget}
along the twelve sightlines. Three features organize the sample. First, the
intervening term is strongly concentrated: summed over the confirmed
foreground systems of Section~\ref{sec:obs-fg}, $\mathrm{DM_{int}}$ exceeds
$100\,\mathrm{pc\,cm^{-3}}$ on only one sightline (FRB~20230307A,
$241\,\mathrm{pc\,cm^{-3}}$, dominated by the intracluster column of
Section~\ref{sec:dominant-systems}), while the remaining sightlines carry
$\lesssim 85\,\mathrm{pc\,cm^{-3}}$ and four carry no confirmed intervening
column at all. One sightline (FRB~20230814B) crosses a foreground halo in the
galaxy-interior regime ($b \approx 4.6$\,kpc), where the reported column is
capped at its $b=0.1\,R_{\rm vir}$ value rather than extrapolated through the
core. Second, the host residuals of the redshift-constrained sightlines are
modest: $\mathrm{DM_{host}} \lesssim 100\,\mathrm{pc\,cm^{-3}}$ for all but
one (FRB~20240229A, $163\,\mathrm{pc\,cm^{-3}}$), consistent with hosts that
contribute a minority of the extragalactic column. Third, the two
highest-redshift sightlines (FRB~20220310F, FRB~20221203A) return small
negative residuals; as quantified in the table notes, at these redshifts the
Macquart mean dominates the budget, so the negative values bound the
sightline-to-sightline scatter about the cosmological mean rather than
implying an unphysical host. The three sightlines without spectroscopic host
redshifts are excluded from all distance-dependent terms, and the two
sightlines outside the deep-survey footprints have intervening terms that are
unconstrained rather than zero (Table~\ref{tab:budget}, notes p and u).

The predicted intervening pulse broadening from the thin-screen two-phase
model is small everywhere: $\tau_{\mathrm{int}} \le 0.024$\,ms at 1\,GHz,
with the largest values on the sightlines whose confirmed halos are crossed
at the smallest impact parameters. The comparison of these predictions with
the measured scattering times is deferred until the joint fits pass
re-validation.

% TODO(results-budget-scattering): Restore the measured-vs-predicted
% scattering comparison (fig:budget overlay, tau_obs column) only after the
% V1 fit ladder clears.

\input{budget_table}

\subsubsection{Dominant intervening systems}
\label{sec:dominant-systems}

Of the twelve sightlines, only one crosses a foreground cluster within its
characteristic radius. The FRB~20230307A sightline passes at $b/R_{500}=0.83$
from the \citet{WenHan2024} cluster J115120.4$+$714435 ($z=0.200$,
$\log M_{500}=14.1$), for which the two-phase modified-NFW model predicts an
intracluster column of order $160\,\mathrm{pc\,cm^{-3}}$
(Figure~\ref{fig:clusters_icm}). This is the single dominant intervening system
in the sample. The other foreground clusters recovered along this and the two
other cluster-bearing sightlines all lie beyond $R_{500}$, where the modeled
column drops steeply; they are excluded from the budget even where the profile
remains formally nonzero, and none approaches the FRB~20230307A cluster in
predicted contribution.

The confirmed foreground galaxies lie at impact parameters of tens to a few
hundred kpc, in the outer circumgalactic medium rather than the galaxy interior.
Each therefore adds only a small dispersion, and no single galaxy halo dominates
its sightline's intervening column.
The intervening dispersion in this sample is thus concentrated almost entirely on
the one cluster-crossing sightline and is small on the rest; the sightlines
carrying no confirmed within-aperture system are limited by survey depth
(Section~\ref{sec:obs-fg}), not shown to be free of foreground gas.

\FloatBarrier
\subsection{Scintillation and screen attribution}
\label{sec:results-scintillation}

% TODO(results-scintillation): Insert after the scintillation products and
% scattering fits are re-run under the re-validation contract. This subsection
% should connect the decorrelation-bandwidth measurements, single-screen tests,
% and screen-attribution claims only after both ingredients are trusted.
%
% Required before filling:
% - Re-run DSA-band scintillation products under the validation contract.
% - Complete CHIME-band scintillation campaign where needed.
% - Re-establish the scattering fits used in any tau * Delta-nu product.
% - Decide whether scintillation acts as a geometry-selection constraint or as
%   part of a fitted two-screen model.
%
% Candidate future artifact, once trusted:
% - fig:scint_screens

\FloatBarrier
\subsection{Turbulence spectrum and burst multiplicity}
\label{sec:results-alpha}

% TODO(results-turbulence): Insert only after the joint scattering fits pass
% the fit re-validation ladder and the geometry-selection campaign has decided
% which model family is citable per sightline. Do not quote rail classes,
% alpha=4 limits, class fractions, median beta, or campaign-era fit values as
% sky results.
%
% This slot should eventually report:
% - the validated per-sightline turbulence parameters, if any;
% - the model family under which each value is citable;
% - multiplicity decisions that survive re-validation;
% - any morphology-induced bias demonstration that is re-established.
%
% Candidate future artifacts, once trusted:
% - tab:beta
% - fig:whitney_mult

The component-multiplicity audit is already informative as a validation step,
even before the turbulence parameters are restored as citable measurements.
Figure~\ref{fig:jointmodel-pair-whitney} shows the current FRB~20220310F
(\textit{whitney}) two-dimensional data/model/residual panel after the CHIME
side is forced to retain its resolved second pulse. The full set of eleven
current joint-model morphology-audit panels is included in
Appendix~\ref{app:jointmodel-pairs}. We use these figures to check whether the
adopted component count leaves coherent burst power in the residuals; we do not
yet quote the fitted scattering or turbulence parameters from them.

\FloatBarrier
\subsection{Band-restricted burst energies}
\label{sec:burst-energies}

% TODO(results-energies): Insert only after the energy re-validation pass.
% A citable row requires a trusted host redshift, trusted per-band spectral
% amplitudes, a checked calibration path, and a resolved inclusion rule.
%
% Required before filling:
% - Re-establish c0 and gamma values from trusted fits or an energy-specific
%   trusted export.
% - Resolve any prior-rail or selection-rule issues.
% - State the burst list and exclusion reasons explicitly.
%
% Candidate future artifact, once trusted:
% - tab:burst-energies
