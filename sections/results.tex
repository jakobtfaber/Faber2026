\section{Results}
\label{sec:results}

The association tests, the foreground census, and the dispersion-measure
budget are presented first, followed by the scattering, scintillation,
turbulence, and energy analyses.

\FloatBarrier
\subsection{Association of the co-detections}
\label{sec:results-association}

All twelve candidate pairs pass the association tests of
Section~\ref{sec:toa}. Under the deliberately conservative windows of
Section~\ref{sec:toa-pcc}, chosen to maximize the estimated chance rate,
every pair has $P_{\rm cc}<10^{-6}$ and passes the
positional-coincidence check; the
timing residuals, referenced to
400\,MHz under the shared DSA-DM convention, are consistent with the
predicted inter-site geometric delay (Table~\ref{tab:sample};
Figure~\ref{fig:association-summary}). The original association calculation
used a DM-conditioned chance term for eight events and deliberately retained
$f_{\rm DM}=1$ for the other four. The new phase-coherence analysis now yields
independent CHIME and DSA measurements for all twelve, with
$|\mathrm{DM_{CHIME}}-\mathrm{DM_{DSA}}|\leq
0.117\,\mathrm{pc\,cm^{-3}}$ (Table~\ref{tab:dm-measurements}). We do not use
this post-selection result to reduce the published chance probabilities for
the four conservative position-and-time associations. We therefore treat all
twelve pairs as genuine co-detections in what follows.

\begin{figure*}[t]
    \centering
    \fbox{\parbox{0.92\textwidth}{\centering\vspace{1.6cm}
    \textbf{[AUTHOR REVIEW PENDING --- ASSOCIATION SUMMARY]}\\[0.6em]
    Candidate must show the revised CHIME--DSA DM comparison and preserve the
    pre-specified association-class semantics. Promotion requires approval of
    the exact candidate SHA-256.\\
    \vspace{1.6cm}}}
    \caption{Reserved association-summary slot. \emph{Draft placeholder---the
    current replacement is not author-approved and is not compiled here.}}
    \label{fig:association-summary}
\end{figure*}

\subsection{Per-sightline dispersion-measure budget}
\label{sec:results-budget}
\label{sec:budget}

Table~\ref{tab:budget} reports the decomposition of Eq.~\ref{eq:dmbudget}
along the twelve sightlines. Three features organize the sample. First, the
intervening term is confined to the four sightlines with confirmed,
budget-eligible foreground systems (Section~\ref{sec:obs-fg}):
FRB~20230307A ($243\,\mathrm{pc\,cm^{-3}}$, dominated by the intracluster
column of Section~\ref{sec:dominant-systems}), FRB~20240229A
($117\,\mathrm{pc\,cm^{-3}}$ from two outer-CGM halos), FRB~20240203A
($26\,\mathrm{pc\,cm^{-3}}$), and FRB~20220310F ($6\,\mathrm{pc\,cm^{-3}}$).
The remaining eight sightlines tabulate $\mathrm{DM_{int}}=0$ because they
carry no confirmed budget-eligible system in the census;
none of these eight positions is constrained by the deep-imaging footprints
(DESI Legacy DR8-North, or SDSS DR12 with usable redshifts), so their
intervening terms are unconstrained by the searched surveys rather than
measured to be zero (note~u), and three of them additionally lack a
spectroscopic host redshift against which any candidate could be classified
(note~p). FRB~20240203A also lies outside the deep footprints (note~u): its
$26\,\mathrm{pc\,cm^{-3}}$ comes from a shallow-layer (NED) confirmed
system, so it is a lower bound on that sightline's intervening column rather
than a complete census.
Second, the forward-modeled host posteriors
(Section~\ref{sec:dm}, Appendix~\ref{app:host-forward-model}) are broad and
asymmetric, dominated by the cosmological scatter: their medians span
$\mathrm{DM_{host}}\approx-10$--$171\,\mathrm{pc\,cm^{-3}}$, the largest on
FRB~20230913A ($171^{+65}_{-81}\,\mathrm{pc\,cm^{-3}}$), and every
redshift-known sightline is consistent with a host contributing a minority of
the extragalactic column once the width of the cosmological term is folded in.
Third, one high-redshift sightline (FRB~20220310F, $z=0.48$) has a host
posterior consistent with zero ($P(\mathrm{DM_{host}}<0)\approx0.5$;
Table~\ref{tab:budget}); its marginally negative median reflects
sightline-to-sightline scatter about the diffuse cosmic normalization at
these redshifts, not an unphysical host, and a small downward shift of the
IGM baryon fraction within its quoted uncertainty places the median at zero.
The highest-redshift sightline (FRB~20230814B, using the
provisional internal value $z=0.5535$) is
likewise dominated by the cosmological term and consistent with zero at
$P(\mathrm{DM_{host}}<0)=0.19$. The three sightlines without
spectroscopic host redshifts are excluded from all distance-dependent terms
(Table~\ref{tab:budget}, note p).

The predicted intervening pulse broadening from the thin-screen two-phase
model is small on all four sightlines with a nonzero intervening column:
$\tau_{\mathrm{int}} \le 0.073$\,ms at 1\,GHz. The comparison of these
predictions with
the measured scattering times is deferred to the scattering analysis
(Section~\ref{sec:results-alpha}).

% TODO(results-budget-scattering): Restore the measured-vs-predicted
% scattering comparison (fig:budget overlay, tau_obs column) only after the
% V1 fit ladder clears.
% Draft figure metadata: analysis/docs/rse/specs/notes/figure-wishlist.md (W1), gate V1+C+D1.
% Keep the placeholder out of the compiled manuscript until the gate clears.

\input{budget_table}

\subsubsection{Dominant intervening systems}
\label{sec:dominant-systems}

Of the twelve sightlines, only one crosses a foreground cluster within its
characteristic radius $R_{500}$, the radius within which the mean enclosed
density is 500 times the critical density at the cluster redshift
(Section~\ref{sec:obs-fg}). The FRB~20230307A sightline passes at $b/R_{500}=0.83$
from the \citet{WenHan2024} cluster J115120.4$+$714435 ($z=0.200$,
$\log M_{500}=14.1$), for which the two-phase modified-NFW model predicts an
intracluster column of order $180\,\mathrm{pc\,cm^{-3}}$
(Figure~\ref{fig:clusters_icm}). The column is systematics-dominated: an
X-ray/SZ-motivated isothermal $\beta$-model with the same $M_{500}$
and a cosmic gas fraction gives a higher central value
($\approx250\,\mathrm{pc\,cm^{-3}}$), and propagating the richness-based
$M_{500}$ ($\sim\!0.2$\,dex scatter), the gas fraction ($f_{\rm gas}=0.10$--$0.16$),
and the profile shape spans $\approx80$--$330\,\mathrm{pc\,cm^{-3}}$---a
factor of $\sim$4 end to end (Appendix~\ref{app:host-forward-model}). The
upper end is capped by the ROSAT All-Sky Survey non-detection of the
cluster: the position-local count-rate limit implies
$L_X<9\times10^{43}\,\mathrm{erg\,s^{-1}}$ (0.1--2.4\,keV) and, through the
MCXC $L_{500}$--$M_{500}$ relation \citep{Piffaretti2011},
$M_{500}\lesssim1.7\times10^{14}\,M_\odot$, truncating the richness prior's
upper tail; the Planck PSZ2 non-detection is uninformative at this mass and
redshift. The mNFW profile is calibrated on
$\sim\!L_\star$ halos \citep{ProchaskaZheng2019} and is not guaranteed to hold at
$\log M_{500}=14.1$; its value sits below the $\beta$-model central estimate
and should be read as a conservative one. This is nonetheless the single
dominant intervening system in the sample. The other foreground clusters recovered along this and the two
other cluster-bearing sightlines all lie beyond $R_{500}$, where the modeled
column drops steeply; they are excluded from the budget even where the profile
remains formally nonzero, and none approaches the FRB~20230307A cluster in
predicted contribution.

Because the $b\le R_{500}$ aperture is a fiducial analysis choice rather
than a physical edge, we state its sensitivity explicitly. Relaxing the
aperture to $1.5\,R_{500}$ admits exactly one additional system in the
entire sample---the FRB~20230307A near-miss J115128.2$+$713637 at
$b/R_{500}=1.25$, whose modeled column is $\approx90\,\mathrm{pc\,cm^{-3}}$
(Figure~\ref{fig:clusters_icm})---raising that sightline's intervening term
from $243$ to $\approx330\,\mathrm{pc\,cm^{-3}}$ and moving its host
posterior from $88^{+88}_{-108}$ to consistency with zero; no further
cluster enters out to $2\,R_{500}$ (the next crossing is at
$b/R_{500}=3.0$). The budget's qualitative structure---intervening DM
dominated by the FRB~20230307A cluster field---is therefore insensitive to
the aperture, while the host attribution on that single sightline weakens
under the wider aperture; we retain $R_{500}$ as the fiducial cut and carry
the near-miss as a one-sided systematic on that sightline's
$\mathrm{DM_{int}}$.

The contributing confirmed foreground galaxies lie at impact parameters of
$\sim$100--270\,kpc, in the outer circumgalactic medium rather than the galaxy
interior, with individual two-phase columns of
$\approx6$--$76\,\mathrm{pc\,cm^{-3}}$ set by the empirical halo masses
(Section~\ref{sec:obs-fg}); the largest single-galaxy column
($76\,\mathrm{pc\,cm^{-3}}$, toward FRB~20240229A) belongs to a
$\log M_\star\approx10.9$ halo pierced at $b/R_{\rm vir}=0.46$.
The intervening DM in this sample is thus dominated by the one
cluster-crossing sightline, with a secondary galaxy-group-scale contribution
toward FRB~20240229A and small columns elsewhere; the sightlines
carrying no confirmed within-aperture system are limited by survey depth
(Section~\ref{sec:obs-fg}), not shown to be free of foreground gas. That
dominance describes a single alignment identified a posteriori in a census
whose depth varies across the sample; it is not a population statement about
FRB sightlines. A priori, the expected number of chance crossings within
$R_{500}$ of a $\ge1.5\times10^{14}\,M_\odot$ cluster across the nine
redshift-constrained sightlines is $N_{\rm exp}\approx0.08$
(\citealt{Tinker2008} halo mass function; exchanging it for that of
\citealt{Despali2016} or varying the redshift bookkeeping shifts this by
$\lesssim$10\%, and lowering the threshold to $10^{14}\,M_\odot$ raises it to
$\approx0.13$), so the single observed alignment is consistent with a
coverage-weighted accident.

\FloatBarrier
\subsection{Scintillation and screen attribution}
\label{sec:results-scintillation}

We report two certified scintillation results: one DSA-band measurement
(FRB~20220506D) and one qualified CHIME-band sightline (FRB~20240203A),
together with the fail-closed outcome of the full CHIME campaign. Because the
two certified results belong to different bursts, they do not define a
cross-band frequency scaling or a two-screen constraint. Across the twelve
co-detections, no other DSA bandwidth and no current scattering fit is promoted
as a measurement. The
decorrelation-bandwidth estimator and the two-screen framework---including the
$\tau_{s}\leftrightarrow\nu_{s}$ relation, the two-screen ACF product term,
and the screen-distance constraint---are set out in
Appendix~\ref{app:twoscreen}.

The exploratory DSA campaign fit spectral ACFs with Lorentzian components, but
an unflagged component fit is not by itself a certified astrophysical
measurement. We therefore report only the candidate that passed the dedicated
qualification below; the unqualified numerical catalog is withheld from the
manuscript pending re-examination.

The clean-component label is a fit-quality classification, not by itself a
certified astrophysical measurement. We therefore subjected the strongest
candidate to a dedicated qualification, with all acceptance criteria fixed in
advance, and it passes every test. For the low-frequency
DSA-110 sub-band of FRB~20220506D, centered at $1328.24\,$MHz, we measure
\begin{equation}
    \gamma = 0.446\ \mathrm{MHz}, \qquad
    68\%\ \mathrm{CI} = [0.196,\,0.685]\ \mathrm{MHz}.
\end{equation}
The direct bounded fit is $0.40065\,$MHz; the reported value and interval are
obtained by inverting a deterministic real-background injection response over
13 truth widths (1024 trials). The measurement passes an independent
off-pulse matched-template null, fit-window and low-lag stability checks,
monotonic response calibration, and a resolved-interval requirement. It is the
only presently certified DSA-band scintillation measurement in the sample and,
as a single sub-band point, does not determine a frequency-scaling index.

\begin{figure*}
    \centering
    \fbox{\parbox{0.92\textwidth}{\centering\vspace{1.6cm}
    \textbf{[AUTHOR REVIEW PENDING]}\\
    \textbf{JOINT DSA+CHIME SCINTILLATION SUMMARY}\\[0.6em]
    The replacement candidate shows only the qualified FRB~20220506D DSA-110
    measurement and qualified FRB~20240203A CHIME/FRB sub-band measurements.
    It must not connect the two bursts with a scaling law or screen inference.
    Promotion requires approval of the exact candidate SHA-256.\\
    \vspace{1.6cm}}}
    \caption{Reserved qualified-scintillation summary slot. \emph{Draft
    placeholder---the joint DSA+CHIME replacement is awaiting exact-byte
    manuscript-owner review.}}
    \label{fig:scint-qualified-summary}
\end{figure*}

\paragraph{CHIME-band campaign.} We analyzed both the standard and available
high-resolution up-channelized CHIME/FRB products for all twelve
co-detections. Objective on/off-pulse windows and matched profile weights are
applied to fixed channel slices. The frequency ACF is fit with either one
Lorentzian or an identifiable narrow-plus-broad pair; the two-component model
is adopted only when it improves the BIC by at least six and the broad scale
exceeds the narrow one by a factor of four. Known-truth injections spanning
$0.02$--$3\,$MHz recover the isolated and narrow two-component widths with
median relative error below one percent, adopt the injected two-component
model in every trial, and produce no false split in the envelope-only or
noise controls. These injections validate the fitter, not the sky origin of
any individual feature.

Every real-data record is therefore also required to pass an off-pulse null,
low-lag stability, minimum-three-sub-band support, amplitude and modulation
physicality, and visual morphology review. A failed or inconclusive control is
fail-closed. Under these gates the campaign yields one qualified CHIME-band
sightline: the high-resolution product for FRB~20240203A. Its four resolved
sub-bands give
\begin{align}
\gamma(749.2\,\mathrm{MHz}) &= 0.1136 \pm 0.0296\,\mathrm{MHz}, \\
\gamma(687.2\,\mathrm{MHz}) &= 0.0779 \pm 0.0121\,\mathrm{MHz}, \\
\gamma(661.1\,\mathrm{MHz}) &= 0.0770 \pm 0.0198\,\mathrm{MHz}, \\
\gamma(623.6\,\mathrm{MHz}) &= 0.0641 \pm 0.0104\,\mathrm{MHz}.
\end{align}
The quoted $1\sigma$ errors combine the fit, objective-window, and
finite-scintle terms in quadrature. A power law across these four CHIME
sub-bands gives a descriptive within-band slope
$\gamma\propto\nu^{3.09\pm0.99}$. The off-pulse width differs from the
on-pulse reference by a factor 2.91, the width remains stable when the lowest
lags are excised, all four sub-bands pass support and shape gates, and the
upstream ACF figure passes morphology review.

The other 23 standard/high-resolution product records remain diagnostic only
with their decisive gates listed in Table~\ref{tab:chime_scint_gates}. This
includes both CHIME products for the other eleven sightlines and the standard
FRB~20240203A product. The earlier FRB~20230325A corrected-product recovery
failure remains part of that diagnostic history, but it no longer justifies a
sample-wide statement that no CHIME measurement exists. Because the qualified
DSA and CHIME measurements are for different bursts, neither is used as the
other's frequency anchor and no $\tau\,\Delta\nu_d$ screen inference is made
from their juxtaposition.

\input{chime_scintillation_campaign_table}

\FloatBarrier
\subsection{Turbulence spectrum and burst multiplicity}
\label{sec:results-alpha}

% TODO(results-turbulence): Insert only after the joint scattering fits pass
% the fit re-validation ladder and the geometry-selection campaign has decided
% which model family is citable per sightline. Do not quote rail classes,
% alpha=4 limits, class fractions, median beta, or campaign-era fit values as
% sky results.
%
% This slot should eventually report:
% - the validated per-sightline turbulence parameters, if any;
% - the model family under which each value is citable;
% - multiplicity decisions that survive re-validation;
% - any morphology-induced bias demonstration that is re-established.
%
% Candidate future artifacts, once trusted:
% - tab:beta
% - fig:subband_tau_validation
% (fig:whitney_mult dropped 2026-07-10 --- outdated multiplicity demo; use
%  appendix jointmodel-pair panels / a re-validated demo if needed later)
%
% Draft figure metadata: analysis/docs/rse/specs/notes/figure-wishlist.md (W4), gate V1.
% Keep it out of the compiled manuscript until the gate clears.

The previously compiled morphology grids and numerical joint-fit catalog have
been removed because they predate the approved power-law
pulse-broadening-function production model. No scattering $\tau$, $\alpha$, or
$\beta$ value is reported until the complete twelve-burst production refit,
model dumps, and review-bound diagnostics clear the fit-validation ladder at a
reviewed pipeline pin.

\FloatBarrier
\subsection{Band-restricted burst energies}
\label{sec:burst-energies}

% V3 status (2026-07-15, validation-v3-energetics-2026-07-15.md): estimator
% re-based on the data-driven per-channel fluence integral (no fitted spectral
% parameters; prior-rail issue thereby moot for energies); inclusion rule made
% explicit below; math/error propagation independently re-verified (ALL PASS).
% NUMBERS REMAIN GATED: the table body fills from the regenerated
% burst_energies.json only after the CHIME-side data-driven fluence run
% completes on the compute host and the owner clears V3. Do not quote the
% legacy (mixed-legacy c0/gamma) energies in prose.

% Draft prose — uncomment only after the CHIME-side data-driven run,
% regenerated-table review, and owner V3 clearance.
%The energy table reports sightlines that satisfy both conditions of the
%inclusion rule stated in Section~\ref{sec:methods-energies}: a spectroscopic
%host redshift, and a validated absolute flux calibration in both bands at the
%burst position. Applying that rule gives eight rows: FRBs~20220207C,
%20220310F, 20220506D, 20221113A, 20230307A, 20230814B, 20230913A, and
%20240203A. Five have published spectroscopic host redshifts. FRB~20230814B
%uses the available project value $z=0.5535$ as a provisional internal
%redshift; FRBs~20230913A and 20240203A also carry as-yet-unpublished
%spectroscopic redshifts and are flagged provisional. The four exclusions are
%FRBs~20221203A, 20230325A, and 20240122A, which lack a host redshift, and
%FRB~20240229A, whose available redshift is photometric rather than
%spectroscopic \citep{Connor2024}. Because the energy estimator uses no
%fitted parameters (Eq.~\ref{eq:eiso}), scattering-model fit quality does not
%enter the selection: an energy row neither requires nor inherits a joint-fit
%quality verdict.

% TODO(fill-at-V3-clear): uncomment and fill from the regenerated table —
% the legacy artifact indicates ~three decades, E_iso ~ 1e38--1e41 erg
% (zach lowest, wilhelm highest), but that range is a revoked-fit product
% until the data-driven regeneration lands; do not compile it before then.
%The resulting band-restricted energies span roughly three decades,
%$E_{\mathrm{iso}}\sim10^{38}$--$10^{41}\,$erg, with the uncertainty budget
%dominated by the absolute-scale systematic rather than by measurement noise
%(Section~\ref{sec:methods-energies}).
%These are observed-band quantities: each row samples a different pair of
%rest-frame intervals, so the rows are not mutually comparable at the
%few-tens-of-percent level, and none is comparable to a fixed-rest-band
%literature energy without a spectral extrapolation we do not perform.

% Table skeleton — fill from the regenerated burst_energies.tex after the
% CHIME data-driven run + owner V3 clearance. Columns deliberately EXCLUDE
% gamma_C/gamma_D (revoked fit products; indices await the C1 campaign).
%\begin{deluxetable*}{lccccc}
%\tablecaption{Band-restricted isotropic-equivalent energies of the
%co-detected FRBs, from the per-channel calibrated fluence integrals over the
%CHIME (0.400--0.800 GHz) and DSA (1.311--1.499 GHz) bands
%(Eq.~\ref{eq:eiso}). Inclusion rule: spectroscopic host redshift and
%validated two-band flux calibration; scattering-fit verdicts do not enter.
%Excluded: FRB 20221203A, FRB 20230325A, FRB 20240122A (no host redshift);
%FRB 20240229A (photometric redshift only). $1\sigma$ uncertainties combine
%per-band statistical fluence errors with the absolute-scale systematics
%($\sim$0.25 dex CHIME, $\sim$0.20 dex DSA) in quadrature.
%\label{tab:burst-energies}}
%\tablehead{\colhead{Burst} & \colhead{$z$} & \colhead{$D_L$ [Mpc]} &
%\colhead{$E_{\rm CHIME}$ [erg]} & \colhead{$E_{\rm DSA}$ [erg]} &
%\colhead{$E_{\rm iso}$ [erg]}}
%\startdata
%...rows from regenerated burst_energies.tex...
%\enddata
%\tablenotetext{a}{Host redshift not published; value provisional
%(FRBs 20230814B, 20230913A, 20240203A).}
%\tablenotetext{b}{DSA absolute scale catalog-anchored only for
%FRBs 20220207C, 20220310F, 20220506D \citep{Law2024}; model-trusted
%otherwise.}
%\end{deluxetable*}
