# Repository archive

Inactive material retained for recovery and audit. Nothing here is a current
manuscript, analysis, code, or knowledge-base input.

- `pre-submodule-placeholders/`: files that occupied `analysis/` before its Git
  submodule could be initialized; only empty directories and Python bytecode.
- `generated-caches/`: generated bytecode with no corresponding source.
