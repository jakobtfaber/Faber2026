# Repository archive

Inactive material retained for recovery and audit. Nothing here is a current
manuscript, analysis, code, or knowledge-base input.

Archive material stays with the repository that owns it:

- parent-only material: `.archive/`;
- analysis material: `analysis/.archive/`;
- pipeline material: `pipeline/.archive/`.

Search and build tools must exclude these directories unless explicitly
auditing historical work.
